<%= form_for :task, url: tasks_path do |f| %>
  <p>
    Task<br>
    <%= f.text_area(:text, {:class => 'form-control'}) %>
  </p>
  <p>
    <%= f.submit({:class => 'btn btn-success'}) %>
  </p>
<% end %>